# Dependencies
node_modules/

# Build output
dist/

# Secrets / env
.env
.env.*

# Loom build cache (artifacts are content-addressed and rebuildable;
# outputs, state, and the event log ARE tracked so snapshots capture results)
**/.loom/cache/
**/.loom/tmp/

# OS / editor
.DS_Store
*.log
